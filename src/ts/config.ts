export default {
  PORT: import.meta.env.VITE_PORT || '',
  SERVER: import.meta.env.VITE_SERVER || "https://kmlivre.ddns.net",

  // PORT: 443,
  // SERVER: 'https://esirene.com.br'

  // PORT: 3001,
  // SERVER: 'https://localhost'
};

// console.log("o .env", import.meta.env.VITE_PORT);
